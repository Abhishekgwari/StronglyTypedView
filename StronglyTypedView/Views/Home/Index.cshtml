// binding  index.cshtml to model class
@* //  @model StronglyTypedView.Models.Employee              // now it is strongly typed view *@

@model IEnumerable<StronglyTypedView.Models.Employee>

@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://learn.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>
@* <h2>@Model.EmpId</h2>                     using Model to access  the data  
<h2>@Model.EmpName</h2>
<h2>@Model.Designation</h2>
<h2>@Model.Salary</h2> *@


                            @* everything stored in model thats why we are writing model to access *@

@{
    foreach (var item in Model)
    {
    <h2>@item.EmpId</h2>                    
    <h2>@item.EmpId</h2>
    <h2>@item.Designation</h2>
    <h2>@item.Salary</h2>
    <p>-----------------------------</p>
    }
}